{{- if and (eq .Values.global.entitlementsMode "license_file") .Values.license.secretName }}
---
# The license secret must be created manually before installing the chart:
#   kubectl create secret generic {{ .Values.license.secretName }} --from-file={{ .Values.license.key }}=./license.jwt
#
# This template only documents the expected secret; it does NOT create it.
# Uncomment the block below if you want Helm to manage a placeholder.
#
# apiVersion: v1
# kind: Secret
# metadata:
#   name: {{ .Values.license.secretName }}
#   labels:
#     {{- include "lamdis.labels" . | nindent 4 }}
# type: Opaque
# data: {}
{{- end }}
