{
  "name": "lamdis-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "lamdis-api",
    "lamdis-runs",
    "lamdis-web"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
